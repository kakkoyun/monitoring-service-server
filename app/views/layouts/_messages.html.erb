<%- unless flash.empty? -%>
    <script type="text/javascript" data-turbolinks-eval=always>
      <% flash.each do |name, message| %>
      toastr['<%= name.to_s.gsub('alert', 'error').gsub('notice', 'info') %>']('<%= message %>');
      <% end %>
    </script>
<%- end -%>
