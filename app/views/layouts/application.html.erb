<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "Monitoring Service Application" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Monitoring Service Application" %>">
  <%= favicon_link_tag -%>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= content_for :head %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<style type="text/css">
  * {
    /*-webkit-touch-callout: none;*/
    /*-webkit-user-select: none;*/
  }
</style>
<body id="<%= controller_name %>">
<div id="wrapper">
  <header>
    <%= render 'layouts/navigation' %>
  </header>
  <main role="main">
    <%= render 'layouts/messages' %>
    <section>
      <% if content_for? :header %>
          <div class="page-header">
            <%= yield :header %>
          </div>
      <% end -%>
      <%= yield %>
    </section>
  </main>
</div>
</body>
</html>

